import React from "react";
import "./LiveMap.css";

const LiveMap = () => {
  return (
    <main className="live-map">
      <h1>Live Map</h1>
      <p>
        Live hazard map will go here. Replace this placeholder with your
        real LiveMap content (e.g. a Leaflet map).
      </p>
    </main>
  );
};

export default LiveMap;